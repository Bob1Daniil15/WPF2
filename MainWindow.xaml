<Window x:Class="WpfApp2_777.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp2_777"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Введите имя:" Grid.Row="0" Margin="5"/>
        <TextBox x:Name="txtName" Grid.Row="1" Margin="5"/>

        <TextBlock Text="Введите пароль:" Grid.Row="1" Margin="5"/>
        <PasswordBox x:Name="txtPassword" Grid.Row="2" Margin="5"/>

        <Button Content="Зарегистрироваться" Click="RegisterButton_Click" Grid.Row="3" Margin="5"/>
        <TextBlock x:Name="txtResult" Grid.Row="4" Margin="5"/>

    </Grid>
</Window>
